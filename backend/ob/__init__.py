"""omababel backend package.

Pure-stdlib Python helper that the Quickshell panel talks to over a tiny
JSON request/response protocol (see ``backend/omababel.py``).  Everything
in here is deliberately dependency free so the plugin stays self-contained.
"""

__version__ = "1.2.0"
